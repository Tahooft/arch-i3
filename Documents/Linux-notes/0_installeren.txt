
pacman -S intel-ucode

pacman -S Qjournalctl (systemctl enable tlp.service)

## Enforce a delay after a failed login attempt
Add the following line to /etc/pam.d/system-login to add a delay of at least 4 seconds between failed login attempts: 

# enable a simple firewall
pacman -s gufw
sudo ufw enable
(firewalld   firewall - nu op thold)

# Kan eruit?
(file roller) archive manager
kvantum manager

# failed login → 4seconds waiting
/etc/pam.d/system-login
auth optional pam_faildelay.so delay=4000000

# SSH can be configured to deny remote logins with the root user by editing the "Authentication" section in the daemon configuration file. Simply set PermitRootLogin to no: 
/etc/ssh/sshd_config
PermitRootLogin no

# Bekijken
grep -r . /sys/devices/system/cpu/vulnerabilities/

# Battery savings
# Nu op laptop de ltp.service enabled en xfce-powermanager for settings (Conflict?)

## Lees: https://wiki.archlinux.org/title/Laptop

# Filesystem monitor plugin for the Xfce4 panel
xfce4-fsguard-plugin

# fingerprint reader voor laptop – nog niet werkend te krijgen...
fingerprint-gui

https://www.youtube.com/watch?v=s0SyDa70l6w --- 10 best

https://github.com/CyberShadow/aconfmgr      slaat alle Arch configs op

wireshark	monitor netwerk
kismet https://www.kismetwireless.net/docs/readme/intro/	Ontdekt alle wifi's  
https://unix4lyfe.org/darkstat/		monitor report all eth0 traffic

firewalld   firewall - nu op thold

lynis - security audit (sudo lynis audit system)
arch-audit
rkhunter (rootkit hunter)


opennic-up	add nameservers (lynis audit advices more then one nameserver...)

goverlay - framerate van games 

obsidian - notes / brain / 

linphone - voip on pc and mobile


https://www.gfinityesports.com/war-thunder/review-is-it-good-worth-playing/
War Thunder on Steam


ncdu - tui voor overzicht diskruimte

 
