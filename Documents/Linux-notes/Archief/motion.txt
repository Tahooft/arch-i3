Motion
https://www.maketecheasier.com/setup-motion-detection-webcam-ubuntu/

sudo apt-get install motion
mkdir .motion
sudo cp /etc/motion/motion.conf ~/.motion/motion.conf
sudo gedit ~/.motion/motion.conf
daemon on			als je wilt dat monitor opstart met Ubuntu

starten met sudo motion

stoppen met
pgrep motion			geeft proces id
kill 1234pgrep 

sudo /etc/init.d/motion restart

beter is:
Edit /etc/default/motion
start_motion_daemon=yes

sudo systemctl enable motion

sudo service motion start / stop /enz

